# Application Configuration
spring.application.name=rsa-jwt-java

# Server Configuration
server.port=8080

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.rsajwt=DEBUG

# Spring Security
# WARNING: These credentials are for demo purposes only
# In production, remove these and use environment variables or secure configuration
spring.security.user.name=admin
spring.security.user.password=admin123


security.jwt.private-key=classpath:certs/private.pem
security.jwt.public-key=classpath:certs/public.pem